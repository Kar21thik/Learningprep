# 05 — Semantic Search

## Subtopics
- Dense retrieval end-to-end
- Query embedding → similarity search → top-K results
- Certainty vs distance score interpretation
- When semantic search fails (keyword-heavy queries, rare terms)

## What You'll Build
- Full semantic search pipeline: embed query → search vector DB → return top-K with scores
- Compare results for keyword queries vs conceptual queries

## Interview Angles
- What is semantic search vs keyword search?
- Dense vs sparse retrieval — when does each win?
- What is top-K and how do you pick K?
- What score threshold makes sense for filtering results?

## Notes
