# 06 — Hybrid Search

## Subtopics
- BM25 (sparse retrieval) — how it works, IDF weighting
- Dense vectors (semantic) vs sparse (BM25)
- Alpha parameter: blending sparse + dense scores
- When hybrid beats pure semantic (exact product names, codes, jargon)
- Reciprocal Rank Fusion (RRF) as an alternative blend

## What You'll Build
- Hybrid search with tunable alpha parameter
- Side-by-side comparison: pure semantic vs pure BM25 vs hybrid on same queries

## Interview Angles
- What is BM25 and how does it differ from TF-IDF?
- What is hybrid search and when should you use it?
- What does the alpha parameter control in hybrid search?
- What is Reciprocal Rank Fusion?

## Notes
